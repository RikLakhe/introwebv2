var letter = prompt('one letter');

var small = letter.toLocaleLowerCase();

var numb = small.charCodeAt(0) - 97 + 1;

for (var i = 0; i < numb; i++) {
    var show = letter + show;
}
console.log(show);
console.log(numb);